datasource db {
  provider = "postgresql"
  url = "***"
}

generator client {
  provider = "prisma-client-js"
}

model Ppl {
  id   String @default(cuid()) @id
  name String
}

model Product1 {
  id   String @default(cuid()) @id
  name String
}