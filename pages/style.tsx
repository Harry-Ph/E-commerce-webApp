// @ts-ignore
import { makeStyles, createStyles, Theme} from '@material-ui/core'

export const useStylesApp = makeStyles(() =>
    createStyles({
        contentApp: {
            minHeight: '660px'
        },
    })
)